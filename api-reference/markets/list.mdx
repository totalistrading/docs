---
title: 'List markets'
description: 'List active Kalshi and Polymarket markets, grouped by event, with filters for category, subcategory, frequency, date window, and full-text search.'
openapi: 'GET /markets'
---

<Note>
  **Building parlays.** Every market carries an `exclusion_keys` array. Two
  markets cannot share a parlay if and only if their `exclusion_keys`
  intersect. A plain set-intersection check is all you need, with no
  correlation rule tables to maintain client-side. The keys encode every rule:
  cross-series groups, threshold ladders, same-game sports correlation, and
  Polymarket sub-events.
</Note>
