---
title: 'List RFQs'
description: 'List your Totalis RFQs with optional status filtering (comma-separated values are accepted). Pass `include=quotes` to embed the quotes array on each row; otherwise `quotes` is null. Cursor-paginated.'
openapi: 'GET /rfqs'
---
