---
title: 'Create API Key'
description: 'Create a new API key for your Totalis account. The full secret is returned only on this response — save it immediately. Requires a Privy JWT (web dashboard); cannot be called with an existing API key.'
openapi: 'POST /v1/api-keys'
---
