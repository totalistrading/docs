---
title: 'Partner Integration'
description: 'Two models for integrating trading on Totalis — managed custody, available today, and headless non-custodial, in development — and how to choose between them.'
---

<Note>
  This section is internal and unpublished. It lives in the docs repo but is not part of the public navigation; share individual pages by direct link. See [Gating](#gating) for how to make it a restricted public section later.
</Note>

Totalis supports two integration models. Both expose the same trading surface — markets, quoting, and settlement — and differ only in who holds the signing keys for the underlying Solana wallet.

| | Managed (custodial) | Headless (non-custodial) |
| --- | --- | --- |
| Status | Available today | In development |
| Keys held by | Totalis-managed wallet | You or your end-users |
| Onboarding | One-time delegation | Fully programmatic |
| Best for | Individuals, quick clients | Platforms that custody their own users |

## Choosing a Model

**Managed** is the fastest path. Totalis provisions and custodies an embedded wallet; you fund it and trade with an API key. The only non-programmatic step is a one-time delegation that authorizes Totalis to sign on the wallet's behalf.

**Headless** keeps custody with you. You, or your end-users, hold the keys. Totalis builds each transaction, you sign it with your own custody stack, and Totalis broadcasts and settles. There is no delegation and no browser step at any point. A single API principal manages any number of sub-accounts, one per end-user, each with its own vault. This is the model for partners running their own platform.

<CardGroup cols={2}>
  <Card title="Managed Quickstart" href="/_internal/partners/quickstart-managed">
    Trade today with a Totalis-managed wallet and an API key.
  </Card>
  <Card title="Headless Integration" href="/_internal/partners/headless-integration">
    Keep custody of your keys while Totalis builds and settles. Preview.
  </Card>
</CardGroup>

Both models share the same base URL and authentication:

- Base URL: `https://api.totalis.trade`
- Authentication: an API key sent in the `X-API-Key` header on every request.

## Gating

This section is currently unpublished — present in the repo but absent from `docs.json`, following the same convention as `_internal/admin-reference`. To turn it into a restricted public section:

1. Partner-facing authentication in Mintlify (Password, JWT, or OAuth) requires the Enterprise plan; the standard tier authenticates only organization members.
2. On Enterprise, move these pages into the `docs.json` navigation as a Partners group and mark the public groups `"public": true`. The Partners group stays gated by the configured method.
3. JWT is the natural fit, tied to the same identity that issues API keys.

Until then, the section remains unpublished and is shared by direct link.
